import 'package:flutter/material.dart';

class PdfNotFoundPage extends StatelessWidget {
  const PdfNotFoundPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Coming Soon',style: TextStyle(color: Colors.black),)),
    );
  }
}
